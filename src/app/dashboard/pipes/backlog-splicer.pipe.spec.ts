import { BacklogSplicerPipe } from './backlog-splicer.pipe';

describe('BacklogSplicerPipe', () => {
  it('create an instance', () => {
    const pipe = new BacklogSplicerPipe();
    expect(pipe).toBeTruthy();
  });
});
